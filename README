Distribution names

Model parameters

inputNodeNumber (integer [0, inf>) - number of input nodes (atomic components)
usedInputNumber (integer [0, inf>) - number of input nodes used in each model
levelCount (integer [2, inf>) - number of levels in each model 
levelNodeCount (integer [1, inf>) - number of nodes on each level
generatorBehaviorLesser (integer [1, 3]) - controls generator behavior when L < P (number of nodes lesser than number of connections)
generatorBehaviorGreater (integer [1, 2]) - controls generator behavior when L > P (number of nodes greater than number of connections)
targetLevel (integer [1, inf>) - level at wich node's edge will be connected (relative to node's level)
nodeType (integer [1, 4]) - type of created inner node

Node parameters

initialReliability (real [0, 1]) - initial reliability of input nodes
nodeWeight (real [0, 1]) - node weight
branchProbability (real [0, 1]) - probability of each branch in branch node
paralleParameter (integer [1, inf>) - k parameter of parallel node
